v3 — command-based UX (no inline category lists)
- Free-text Problem & Plan (no presets)
- Asks Truck/Trailer, then asks for Truck#/Trailer# (free text, you can type Skip)
- Pure Telegram commands (/, no inline buttons inside messages):
  /new, /submit, /my, /update <id>, /status <id> <value>, /eta <id> <...>, /snooze <id> <hours>, /addphoto <id>, /close <id>
- Built-in hourly reminders + /cron endpoint
